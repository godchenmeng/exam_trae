<Window x:Class="ExamSystem.WPF.TestOptionDisplay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选项显示测试" Height="600" Width="800">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <Style x:Key="OptionRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style x:Key="OptionCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 控制按钮 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Name="btnSingleChoice" Content="显示单选题" Click="ShowSingleChoice_Click" Margin="0,0,10,0"/>
            <Button Name="btnMultipleChoice" Content="显示多选题" Click="ShowMultipleChoice_Click" Margin="0,0,10,0"/>
            <Button Name="btnTrueFalse" Content="显示判断题" Click="ShowTrueFalse_Click"/>
        </StackPanel>
        
        <!-- 状态显示 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="当前题目类型: " FontWeight="Bold"/>
            <TextBlock Name="txtQuestionType" Text="无"/>
            <TextBlock Text="  选项数量: " FontWeight="Bold" Margin="20,0,0,0"/>
            <TextBlock Name="txtOptionCount" Text="0"/>
        </StackPanel>
        
        <!-- 选项显示区域 -->
        <Border Grid.Row="2" Background="White" BorderBrush="#DEE2E6" BorderThickness="1" 
                CornerRadius="8" Padding="25">
            <StackPanel Name="AnswerPanel">
                <!-- 单选题 -->
                <ItemsControl Name="SingleChoiceOptions" 
                              Visibility="{Binding IsSingleChoice, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <RadioButton Content="{Binding Text}" 
                                         GroupName="SingleChoice"
                                         Style="{StaticResource OptionRadioButtonStyle}"
                                         IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- 多选题 -->
                <ItemsControl Name="MultipleChoiceOptions" 
                              Visibility="{Binding IsMultipleChoice, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Text}" 
                                      Style="{StaticResource OptionCheckBoxStyle}"
                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- 判断题 -->
                <StackPanel Name="TrueFalseOptions" 
                            Visibility="{Binding IsTrueFalse, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <RadioButton Content="✓ 正确" GroupName="TrueFalse" Style="{StaticResource OptionRadioButtonStyle}"/>
                    <RadioButton Content="✗ 错误" GroupName="TrueFalse" Style="{StaticResource OptionRadioButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>