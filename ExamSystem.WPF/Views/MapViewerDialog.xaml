<Window x:Class="ExamSystem.WPF.Views.MapViewerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="在地图上查看建筑" Height="720" Width="1200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#f8f9fa">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部信息栏 -->
        <!-- <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock x:Name="txtTitle" Text="建筑：-" FontSize="16" FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <TextBlock Text="  " />
            <TextBlock x:Name="txtCity" Text="城市：-" VerticalAlignment="Center"/>
            <TextBlock Text="  " />
            <TextBlock x:Name="txtCoord" Text="坐标：-" VerticalAlignment="Center"/>
        </DockPanel> -->

        <!-- WebView2 展示地图 -->
        <Grid Grid.Row="1" Background="#ffffff" ClipToBounds="True">
            <wv2:WebView2 x:Name="webView"/>
            <Border BorderBrush="#dee2e6" BorderThickness="1" />
        </Grid>

        <!-- 底部按钮栏 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="关闭" Padding="16,8" Click="OnCloseClick"/>
        </StackPanel>
    </Grid>
</Window>