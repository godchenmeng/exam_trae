<UserControl x:Class="ExamSystem.WPF.Views.ExamResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <UserControl.Resources>
        <!-- 样式定义 -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        
        <Style x:Key="ScoreTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
        
        <Style x:Key="PassedScoreStyle" TargetType="TextBlock" BasedOn="{StaticResource ScoreTextStyle}">
            <Setter Property="Foreground" Value="#27AE60"/>
        </Style>
        
        <Style x:Key="FailedScoreStyle" TargetType="TextBlock" BasedOn="{StaticResource ScoreTextStyle}">
            <Setter Property="Foreground" Value="#E74C3C"/>
        </Style>
        
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#21618C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#95A5A6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7F8C8D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#566573"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="#ECF0F1">
        <!-- 加载指示器 -->
        <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Rectangle Fill="White" Opacity="0.8"/>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,10"/>
                <TextBlock Text="正在加载考试结果..." HorizontalAlignment="Center" FontSize="14" Foreground="#7F8C8D"/>
            </StackPanel>
        </Grid>
        
        <!-- 主内容 -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                      Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <StackPanel Margin="20">
                <!-- 页面标题 -->
                <TextBlock Text="考试结果" Style="{StaticResource HeaderTextStyle}" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <!-- 错误消息 -->
                <Border Background="#E74C3C" CornerRadius="3" Padding="10" Margin="0,0,0,10"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="White" FontWeight="Bold"/>
                </Border>
                
                <!-- 考试结果卡片 -->
                <Border Style="{StaticResource CardStyle}" Visibility="{Binding HasExamRecord, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 考试基本信息 -->
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="考试信息" Style="{StaticResource HeaderTextStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Style="{StaticResource InfoTextStyle}">
                                        <Run Text="试卷名称：" FontWeight="Bold"/>
                                        <Run Text="{Binding ExamPaperName}"/>
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource InfoTextStyle}">
                                        <Run Text="考试时间：" FontWeight="Bold"/>
                                        <Run Text="{Binding ExamDateText}"/>
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource InfoTextStyle}">
                                        <Run Text="用时：" FontWeight="Bold"/>
                                        <Run Text="{Binding TimeSpentText}"/>
                                    </TextBlock>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1">
                                    <TextBlock Style="{StaticResource InfoTextStyle}">
                                        <Run Text="总分：" FontWeight="Bold"/>
                                        <Run Text="{Binding TotalScoreText}"/>
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource InfoTextStyle}">
                                        <Run Text="及格分：" FontWeight="Bold"/>
                                        <Run Text="{Binding PassScoreText}"/>
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource InfoTextStyle}">
                                        <Run Text="状态：" FontWeight="Bold"/>
                                        <Run Text="{Binding ExamStatusText}" Foreground="{Binding StatusColor}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        
                        <!-- 成绩显示 -->
                        <StackPanel Grid.Row="1" Margin="0,20,0,0">
                            <TextBlock Text="考试成绩" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding ScoreText}" 
                                       Style="{Binding IsPassed, Converter={StaticResource PassedFailedStyleConverter}}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding ResultText}" 
                                       FontSize="16" 
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       Foreground="{Binding StatusColor}"/>
                        </StackPanel>
                        
                        <!-- 统计信息 -->
                        <StackPanel Grid.Row="2" Margin="0,20,0,0">
                            <TextBlock Text="答题统计" Style="{StaticResource HeaderTextStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding TotalQuestions}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#3498DB"/>
                                    <TextBlock Text="总题数" FontSize="12" HorizontalAlignment="Center" Foreground="#7F8C8D"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding CorrectAnswers}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#27AE60"/>
                                    <TextBlock Text="正确" FontSize="12" HorizontalAlignment="Center" Foreground="#7F8C8D"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding WrongAnswers}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#E74C3C"/>
                                    <TextBlock Text="错误" FontSize="12" HorizontalAlignment="Center" Foreground="#7F8C8D"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding AccuracyRateText}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#9B59B6"/>
                                    <TextBlock Text="正确率" FontSize="12" HorizontalAlignment="Center" Foreground="#7F8C8D"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- 答题详情 -->
                <Border Style="{StaticResource CardStyle}" Margin="0,10,0,0" 
                        Visibility="{Binding HasAnswerRecords, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="答题详情" Style="{StaticResource HeaderTextStyle}"/>
                        
                        <DataGrid ItemsSource="{Binding AnswerRecords}" 
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  Background="White"
                                  RowBackground="White"
                                  AlternatingRowBackground="#F8F9FA">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="题号" Binding="{Binding QuestionNumber}" Width="60"/>
                                <DataGridTextColumn Header="题目类型" Binding="{Binding QuestionTypeText}" Width="100"/>
                                <DataGridTextColumn Header="题目内容" Binding="{Binding QuestionContent}" Width="300"/>
                                <DataGridTextColumn Header="我的答案" Binding="{Binding UserAnswerText}" Width="150"/>
                                <DataGridTextColumn Header="正确答案" Binding="{Binding CorrectAnswerText}" Width="150"/>
                                <DataGridTextColumn Header="得分" Binding="{Binding ScoreText}" Width="80"/>
                                <DataGridTemplateColumn Header="结果" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ResultText}" 
                                                       Foreground="{Binding ResultColor}"
                                                       FontWeight="Bold"
                                                       HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
                
                <!-- 操作按钮 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="查看试卷" 
                            Command="{Binding ViewExamPaperCommand}"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="重新考试" 
                            Command="{Binding RetakeExamCommand}"
                            Style="{StaticResource ButtonStyle}"
                            Visibility="{Binding CanRetake, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <Button Content="导出结果" 
                            Command="{Binding ExportResultCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                    <Button Content="返回" 
                            Command="{Binding BackCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>