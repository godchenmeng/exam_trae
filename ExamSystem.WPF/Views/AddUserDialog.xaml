<Window x:Class="ExamSystem.WPF.Views.AddUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加用户" Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Text="添加新用户" FontSize="16" FontWeight="Bold"/>

    <Grid Grid.Row="1" Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="用户名" VerticalAlignment="Center"/>
      <TextBox x:Name="UsernameTextBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="2" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="真实姓名" VerticalAlignment="Center"/>
      <TextBox x:Name="RealNameTextBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="3" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="邮箱" VerticalAlignment="Center"/>
      <TextBox x:Name="EmailTextBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="4" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="电话" VerticalAlignment="Center"/>
      <TextBox x:Name="PhoneTextBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="5" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="角色" VerticalAlignment="Center"/>
      <ComboBox x:Name="RoleComboBox" Grid.Column="1" MinWidth="140">
        <ComboBoxItem Content="管理员" Tag="Admin"/>
        <ComboBoxItem Content="教师" Tag="Teacher"/>
        <ComboBoxItem Content="学生" Tag="Student"/>
      </ComboBox>
    </Grid>

    <Grid Grid.Row="6" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="密码" VerticalAlignment="Center"/>
      <PasswordBox x:Name="PasswordBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="7" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="确认密码" VerticalAlignment="Center"/>
      <PasswordBox x:Name="ConfirmPasswordBox" Grid.Column="1"/>
    </Grid>

    <StackPanel Grid.Row="8" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,8,0,0">
      <CheckBox x:Name="IsActiveCheckBox" Content="启用"/>
      <TextBlock x:Name="ErrorText" Margin="16,0,0,0" Foreground="Red" TextWrapping="Wrap"/>
    </StackPanel>

    <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="保存" Width="80" Margin="0,0,8,0" Click="OnSaveClick"/>
      <Button Content="取消" Width="80" Click="OnCancelClick"/>
    </StackPanel>
  </Grid>
</Window>