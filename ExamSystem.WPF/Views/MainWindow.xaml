<Window x:Class="ExamSystem.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="在线考试系统 - WPF版本" 
        Height="800" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        FontFamily="Microsoft YaHei UI"
        FontSize="14">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" 
                Background="#2196F3" 
                Padding="16">
            <DockPanel>
                <TextBlock Text="📚" 
                          DockPanel.Dock="Left"
                          FontSize="24" 
                          VerticalAlignment="Center" 
                          Margin="0,0,16,0"
                          Foreground="White"/>
                <TextBlock Text="在线考试系统" 
                          FontSize="20" 
                          FontWeight="Bold" 
                          VerticalAlignment="Center"
                          Foreground="White"/>
                
                <StackPanel Orientation="Horizontal" 
                           DockPanel.Dock="Right" 
                           HorizontalAlignment="Right">
                    <Button Content="题库管理"
                            Style="{StaticResource HeaderButton}"
                            Margin="8,0"/>
                    <Button Content="试卷管理"
                            Style="{StaticResource HeaderButton}"
                            Margin="8,0"/>
                    <Button Content="考试管理"
                            Style="{StaticResource HeaderButton}"
                            Margin="8,0"/>
                    <Button Content="设置"
                            Style="{StaticResource HeaderButton}"
                            Margin="8,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- 主内容区域 -->
        <TabControl Grid.Row="1" 
                    Style="{StaticResource ModernTabControl}"
                    Margin="16">
            
            <!-- 仪表板 -->
            <TabItem Header="仪表板" Style="{StaticResource ModernTabItem}" IsSelected="True">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏠" 
                                      FontSize="16" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,4,0"/>
                            <TextBlock Text="仪表板" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <Frame Name="DashboardFrame" NavigationUIVisibility="Hidden"/>
            </TabItem>
            
            <!-- 题库管理 -->
            <TabItem Header="题库管理" Style="{StaticResource ModernTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📖" 
                                      FontSize="16" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,4,0"/>
                            <TextBlock Text="题库管理" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <Frame Name="QuestionBankFrame" NavigationUIVisibility="Hidden"/>
            </TabItem>

            <!-- 试卷管理 -->
            <TabItem Header="试卷管理" Style="{StaticResource ModernTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📄" 
                                      FontSize="16" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,4,0"/>
                            <TextBlock Text="试卷管理" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <Frame Name="ExamPaperFrame" NavigationUIVisibility="Hidden"/>
            </TabItem>

            <!-- 考试管理 -->
            <TabItem Header="考试管理" Style="{StaticResource ModernTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✏️" 
                                      FontSize="16" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,4,0"/>
                            <TextBlock Text="考试管理" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <Frame Name="ExamFrame" NavigationUIVisibility="Hidden"/>
            </TabItem>

            <!-- 考试结果 -->
            <TabItem Header="考试结果" Style="{StaticResource ModernTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" 
                                      FontSize="16" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,4,0"/>
                            <TextBlock Text="考试结果" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <Frame Name="ExamResultFrame" NavigationUIVisibility="Hidden"/>
            </TabItem>

            <!-- 统计报表 -->
            <TabItem Header="统计报表" Style="{StaticResource ModernTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📈" 
                                      FontSize="16" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,4,0"/>
                            <TextBlock Text="统计报表" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <Frame Name="StatisticsFrame" NavigationUIVisibility="Hidden"/>
            </TabItem>

            <!-- 用户管理 -->
            <TabItem Header="用户管理" Style="{StaticResource ModernTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👥" 
                                      FontSize="16" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,4,0"/>
                            <TextBlock Text="用户管理" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <Frame Name="UserManagementFrame" NavigationUIVisibility="Hidden"/>
            </TabItem>
        </TabControl>

        <!-- 状态栏 -->
        <Border Grid.Row="2" 
                Background="#E3F2FD" 
                Padding="16,8">
            <DockPanel>
                <TextBlock Text="就绪" 
                          DockPanel.Dock="Left" 
                          VerticalAlignment="Center"
                          Foreground="#1976D2"/>
                <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='当前时间: {0:yyyy-MM-dd HH:mm:ss}'}" 
                          DockPanel.Dock="Right" 
                          HorizontalAlignment="Right" 
                          VerticalAlignment="Center"
                          Foreground="#1976D2"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>