<Window x:Class="ExamSystem.WPF.Views.ExamPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="考试预览" Height="700" Width="900"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- 样式定义 -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        
        <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="Margin" Value="0,0,15,5"/>
        </Style>
        
        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style x:Key="QuestionTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="LineHeight" Value="20"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2980B9"/>
        </Style>
        
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#229954"/>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#95A5A6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#7F8C8D"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 主要内容区域 -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- 试卷标题 -->
                <TextBlock Text="{Binding ExamPaper.PaperName}" Style="{StaticResource HeaderTextStyle}" 
                           HorizontalAlignment="Center"/>
                
                <!-- 试卷基本信息 -->
                <Border Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="1" 
                        CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="考试时长：" Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding ExamPaper.Duration}" 
                                   Style="{StaticResource ValueTextStyle}" Margin="70,0,0,5"/>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="分钟" Style="{StaticResource ValueTextStyle}" 
                                   Margin="110,0,0,5"/>
                        
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="总分：" Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ExamPaper.TotalScore}" 
                                   Style="{StaticResource ValueTextStyle}" Margin="40,0,0,5"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="分" Style="{StaticResource ValueTextStyle}" 
                                   Margin="70,0,0,5"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="及格分：" Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding ExamPaper.PassScore}" 
                                   Style="{StaticResource ValueTextStyle}" Margin="55,0,0,5"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="分" Style="{StaticResource ValueTextStyle}" 
                                   Margin="85,0,0,5"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="题目数量：" Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ExamPaper.QuestionCount}" 
                                   Style="{StaticResource ValueTextStyle}" Margin="70,0,0,5"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="题" Style="{StaticResource ValueTextStyle}" 
                                   Margin="100,0,0,5"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="允许重考：" Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding AllowRetakeText}" 
                                   Style="{StaticResource ValueTextStyle}" Margin="70,0,0,5"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="随机题目：" Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding RandomQuestionText}" 
                                   Style="{StaticResource ValueTextStyle}" Margin="70,0,0,5"/>
                        
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="开始时间：" Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{Binding StartTimeText}" 
                                   Style="{StaticResource ValueTextStyle}" Margin="70,0,0,5"/>
                        
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="结束时间：" Style="{StaticResource InfoTextStyle}"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding EndTimeText}" 
                                   Style="{StaticResource ValueTextStyle}" Margin="70,0,0,5"/>
                        
                        <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Text="考试说明：" 
                                   Style="{StaticResource InfoTextStyle}" Margin="0,10,0,5"/>
                        <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Text="{Binding ExamPaper.Description}" 
                                   Style="{StaticResource ValueTextStyle}" TextWrapping="Wrap" 
                                   Margin="0,25,0,5"/>
                    </Grid>
                </Border>
                
                <!-- 题目列表 -->
                <TextBlock Text="题目预览" Style="{StaticResource SubHeaderTextStyle}"/>
                
                <ItemsControl ItemsSource="{Binding Questions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#E9ECEF" BorderThickness="1" 
                                    CornerRadius="5" Margin="0,0,0,15" Padding="15">
                                <StackPanel>
                                    <!-- 题目标题 -->
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="第" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding QuestionNumber}" FontWeight="SemiBold"/>
                                        <TextBlock Text="题" FontWeight="SemiBold"/>
                                        <TextBlock Text="（" FontWeight="SemiBold" Margin="10,0,0,0"/>
                                        <TextBlock Text="{Binding QuestionTypeText}" FontWeight="SemiBold"/>
                                        <TextBlock Text="，" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Score}" FontWeight="SemiBold"/>
                                        <TextBlock Text="分）" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    
                                    <!-- 题目内容 -->
                                    <TextBlock Text="{Binding Content}" Style="{StaticResource QuestionTextStyle}"/>
                                    
                                    <!-- 选项 -->
                                    <ItemsControl ItemsSource="{Binding Options}" 
                                                  Visibility="{Binding HasOptions, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" Margin="20,2,0,2" FontSize="13"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- 考试须知 -->
                <Border Background="#FFF3CD" BorderBrush="#FFEAA7" BorderThickness="1" 
                        CornerRadius="5" Padding="15" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="考试须知" Style="{StaticResource SubHeaderTextStyle}" 
                                   Foreground="#856404"/>
                        <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="#856404">
                            <Run Text="1. 考试开始后，系统将自动计时，请合理安排答题时间。"/>
                            <LineBreak/>
                            <Run Text="2. 考试过程中，答案会自动保存，可随时切换题目。"/>
                            <LineBreak/>
                            <Run Text="3. 考试时间到后，系统将自动提交试卷。"/>
                            <LineBreak/>
                            <Run Text="4. 请确保网络连接稳定，避免因网络问题影响考试。"/>
                            <LineBreak/>
                            <Run Text="5. 考试期间请勿关闭浏览器或刷新页面。"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- 底部按钮栏 -->
        <Border Grid.Row="1" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0" 
                Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Style="{StaticResource SecondaryButtonStyle}" 
                        Command="{Binding CancelCommand}"/>
                <Button Content="开始考试" Style="{StaticResource SuccessButtonStyle}" 
                        Command="{Binding StartExamCommand}"
                        IsEnabled="{Binding CanStartExam}"/>
            </StackPanel>
        </Border>
        
        <!-- 加载指示器 -->
        <Grid Grid.RowSpan="2" Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,10"/>
                <TextBlock Text="加载中..." Foreground="White" FontSize="16" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>