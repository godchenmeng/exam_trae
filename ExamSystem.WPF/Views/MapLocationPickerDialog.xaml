<Window x:Class="ExamSystem.WPF.Views.MapLocationPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="地图选点" Height="600" Width="1200"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <wv2:WebView2 x:Name="webView"
                      Grid.Row="0" />

        <DockPanel Grid.Row="1" Margin="8">
            <TextBlock x:Name="txtStatus" VerticalAlignment="Center" />
            <Button Content="关闭" Width="80" Height="28" HorizontalAlignment="Right" DockPanel.Dock="Right" Click="OnCloseClick" />
        </DockPanel>
    </Grid>
</Window>