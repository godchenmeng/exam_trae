<Window x:Class="ExamSystem.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="在线考试系统 - 登录"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- 避免引用未定义的特效资源键 -->
        <DropShadowEffect x:Key="DropShadowEffect" Color="Gray" Direction="270" ShadowDepth="5" Opacity="0.3" BlurRadius="10"/>
        
        <!-- 渐变背景 -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF6A5E" Offset="0"/>
            <GradientStop Color="#C62828" Offset="1"/>
        </LinearGradientBrush>
        
        <!-- LOGO样式 -->
        <Style x:Key="LogoStyle" TargetType="Ellipse">
            <Setter Property="Fill">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FF8A80" Offset="0"/>
                        <GradientStop Color="#FF3B30" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="15" BorderBrush="#E0E0E0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="8" Opacity="0.4" BlurRadius="15"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="{StaticResource BackgroundGradient}" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="在线考试系统 - 登录" 
                              Foreground="White" 
                              FontSize="14" 
                              FontWeight="Bold"
                              VerticalAlignment="Center" 
                              HorizontalAlignment="Left" 
                              Margin="15,0"/>
                    
                    <Button x:Name="CloseButton" 
                           Content="✕" 
                           Width="40" 
                           Height="40" 
                           Background="Transparent" 
                           Foreground="White" 
                           BorderThickness="0" 
                           FontSize="16" 
                           FontWeight="Bold"
                           HorizontalAlignment="Right"
                           Command="{Binding CloseCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="0,15,0,0">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F44336"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- 主内容区域 -->
            <Grid Grid.Row="1" Background="White">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="400"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧装饰区域 -->
                <Border Grid.Column="0" Background="{StaticResource BackgroundGradient}" CornerRadius="0,0,0,15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- LOGO区域 -->
                        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <!-- LOGO图标 -->
                            <!-- 替换原椭圆+emoji 为项目根目录 logo.png -->
                            <Image Source="pack://application:,,,/logo.png" Width="120" Height="120" Stretch="Uniform" Margin="0,0,0,20" Effect="{StaticResource DropShadowEffect}"/>
                            
                            <!-- 系统名称 -->
                            <TextBlock Text="在线考试系统" 
                                      FontSize="32" 
                                      FontWeight="Bold" 
                                      Foreground="White" 
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,10"/>
                            
                            <!-- 英文名称 -->
                            <TextBlock Text="Online Exam System" 
                                      FontSize="16" 
                                      Foreground="White" 
                                      Opacity="0.9"
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,20"/>
                        </StackPanel>
                        
                        <!-- 标语区域 -->
                        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="40,0">
                            <TextBlock Text="智能化考试管理平台" 
                                      FontSize="18" 
                                      Foreground="White" 
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,10"/>
                            <TextBlock Text="让考试更简单，让学习更高效" 
                                      FontSize="14" 
                                      Foreground="White" 
                                      Opacity="0.8"
                                      HorizontalAlignment="Center"
                                      TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 右侧登录表单区域 -->
                <Grid Grid.Column="1" Background="White">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 登录表单 -->
                    <Border Grid.Row="1" 
                           Background="White" 
                           Padding="50,40"
                           Margin="20">
                        
                        <StackPanel>
                            <!-- 登录标题 -->
                            <TextBlock Text="欢迎登录" 
                                      FontSize="28" 
                                      FontWeight="Bold" 
                                      Foreground="#333333" 
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,10"/>
                            
                            <TextBlock Text="请输入您的账号信息" 
                                      FontSize="14" 
                                      Foreground="#666666" 
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,40"/>

                            <!-- 用户名输入 -->
                            <TextBlock Text="用户名" 
                                      FontSize="14" 
                                      Foreground="#666666" 
                                      Margin="0,0,0,8"/>
                            
                            <TextBox x:Name="UsernameTextBox" 
                                    Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                                    Height="45" 
                                    FontSize="14" 
                                    Padding="15,0"
                                    BorderBrush="#E0E0E0" 
                                    BorderThickness="1"
                                    Margin="0,0,0,20">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Border Background="{TemplateBinding Background}" 
                                                           BorderBrush="{TemplateBinding BorderBrush}"
                                                           BorderThickness="{TemplateBinding BorderThickness}"
                                                           CornerRadius="6">
                                                        <ScrollViewer x:Name="PART_ContentHost" 
                                                                     Margin="{TemplateBinding Padding}"
                                                                     VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsFocused" Value="True">
                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="#B0B0B0"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>

                            <!-- 密码输入 -->
                            <TextBlock Text="密码" 
                                      FontSize="14" 
                                      Foreground="#666666" 
                                      Margin="0,0,0,8"/>
                            
                            <PasswordBox x:Name="PasswordBox" 
                                        Height="45" 
                                        FontSize="14" 
                                        Padding="15,0"
                                        BorderBrush="#E0E0E0" 
                                        BorderThickness="1"
                                        Margin="0,0,0,20">
                                <PasswordBox.Style>
                                    <Style TargetType="PasswordBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="PasswordBox">
                                                    <Border Background="{TemplateBinding Background}" 
                                                           BorderBrush="{TemplateBinding BorderBrush}"
                                                           BorderThickness="{TemplateBinding BorderThickness}"
                                                           CornerRadius="6">
                                                        <ScrollViewer x:Name="PART_ContentHost" 
                                                                     Margin="{TemplateBinding Padding}"
                                                                     VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsFocused" Value="True">
                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="#B0B0B0"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </PasswordBox.Style>
                            </PasswordBox>

                            <!-- 记住密码 -->
                            <CheckBox x:Name="RememberPasswordCheckBox" 
                                     IsChecked="{Binding RememberPassword}"
                                     Content="记住密码" 
                                     FontSize="14" 
                                     Foreground="#666666"
                                     Margin="0,0,0,25">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="CheckBox">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border x:Name="CheckBoxBorder"
                                                               Width="18" Height="18"
                                                               BorderBrush="#CCCCCC"
                                                               BorderThickness="1"
                                                               CornerRadius="3"
                                                               Background="White"
                                                               Margin="0,0,8,0">
                                                            <TextBlock x:Name="CheckMark"
                                                                      Text="✓"
                                                                      FontSize="12"
                                                                      Foreground="{StaticResource PrimaryBrush}"
                                                                      HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"
                                                                      Visibility="Collapsed"/>
                                                        </Border>
                                                        <ContentPresenter VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>

                            <!-- 错误信息 -->
                            <TextBlock x:Name="ErrorTextBlock" 
                                      Text="{Binding ErrorMessage}"
                                      Foreground="#F44336" 
                                      FontSize="13" 
                                      TextWrapping="Wrap"
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,20">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </Trigger>
                                            <Trigger Property="Text" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- 登录按钮 -->
                            <Button x:Name="LoginButton" 
                                   Command="{Binding LoginCommand}"
                                   Height="50" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Foreground="White" 
                                   Background="{StaticResource BackgroundGradient}" 
                                   BorderThickness="0"
                                   Cursor="Hand"
                                   IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                           CornerRadius="8"
                                                           BorderBrush="{TemplateBinding BorderBrush}"
                                                           BorderThickness="{TemplateBinding BorderThickness}">
                                                        <Border.Effect>
                                                            <DropShadowEffect Color="#667eea" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                                                        </Border.Effect>
                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                        <GradientStop Color="#5a6fd8" Offset="0"/>
                                                                        <GradientStop Color="#6b4c93" Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                        <GradientStop Color="#4d5fc7" Offset="0"/>
                                                                        <GradientStop Color="#5e4284" Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Background" Value="#CCCCCC"/>
                                                            <Setter Property="Foreground" Value="#999999"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding IsLoading, Converter={StaticResource LoadingTextConverter}}" 
                                                  VerticalAlignment="Center"/>
                                        <ProgressBar Width="20" Height="20" 
                                                   Margin="10,0,0,0"
                                                   IsIndeterminate="True"
                                                   Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- 页脚信息 -->
                    <StackPanel Grid.Row="2" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Bottom"
                               Margin="0,0,0,30">
                        <TextBlock Text="在线考试系统 v1.0" 
                                  FontSize="12" 
                                  Foreground="#999999" 
                                  HorizontalAlignment="Center"/>
                        <TextBlock Text="© 2024 版权所有" 
                                  FontSize="10" 
                                  Foreground="#CCCCCC" 
                                  HorizontalAlignment="Center" 
                                  Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>