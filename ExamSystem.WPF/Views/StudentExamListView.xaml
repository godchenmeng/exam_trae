<UserControl x:Class="ExamSystem.WPF.Views.StudentExamListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ExamSystem.WPF.Converters">
    
    <UserControl.Resources>
        <converters:DifficultyConverter x:Key="DifficultyConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <Style x:Key="ExamCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ExamTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        
        <Style x:Key="ExamInfoStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
        </Style>
        
        <Style x:Key="StartExamButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#21618C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="可参加的考试" Style="{StaticResource HeaderTextStyle}"/>
        
        <!-- 搜索和筛选区域 -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- 搜索框 -->
            <TextBox Grid.Column="0" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource SearchBoxStyle}"
                     Margin="0,0,15,0">
                <TextBox.Resources>
                    <Style TargetType="TextBox" BasedOn="{StaticResource SearchBoxStyle}">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="搜索试卷名称..." 
                                                         Foreground="#95A5A6" 
                                                         FontSize="14" 
                                                         Margin="10,0,0,0"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Resources>
            </TextBox>
            
            <!-- 难度筛选 -->
            <ComboBox Grid.Column="1" 
                      SelectedItem="{Binding SelectedDifficulty}"
                      ItemsSource="{Binding DifficultyOptions}"
                      Style="{StaticResource FilterComboBoxStyle}"
                      Width="120"
                      Margin="0,0,15,0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource DifficultyConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            
            <!-- 状态筛选 -->
            <ComboBox Grid.Column="2" 
                      SelectedItem="{Binding SelectedStatus}"
                      ItemsSource="{Binding StatusOptions}"
                      Style="{StaticResource FilterComboBoxStyle}"
                      Width="120"
                      Margin="0,0,15,0"/>
            
            <!-- 刷新按钮 -->
            <Button Grid.Column="3" 
                    Command="{Binding RefreshCommand}"
                    Content="刷新"
                    Style="{StaticResource StartExamButtonStyle}"
                    Background="#95A5A6"/>
        </Grid>
        
        <!-- 考试列表 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredExamPapers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ExamCardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- 试卷标题 -->
                                <TextBlock Grid.Row="0" 
                                         Text="{Binding Title}" 
                                         Style="{StaticResource ExamTitleStyle}"/>
                                
                                <!-- 试卷信息 -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Style="{StaticResource ExamInfoStyle}">
                                        <Run Text="考试时长: "/>
                                        <Run Text="{Binding Duration}" FontWeight="SemiBold"/>
                                        <Run Text=" 分钟"/>
                                    </TextBlock>
                                    
                                    <TextBlock Style="{StaticResource ExamInfoStyle}">
                                        <Run Text="题目数量: "/>
                                        <Run Text="{Binding QuestionCount}" FontWeight="SemiBold"/>
                                        <Run Text=" 题"/>
                                    </TextBlock>
                                    
                                    <TextBlock Style="{StaticResource ExamInfoStyle}">
                                        <Run Text="总分: "/>
                                        <Run Text="{Binding TotalScore}" FontWeight="SemiBold"/>
                                        <Run Text=" 分"/>
                                    </TextBlock>
                                    
                                    <TextBlock Style="{StaticResource ExamInfoStyle}">
                                        <Run Text="难度: "/>
                                        <Run Text="{Binding Difficulty, Converter={StaticResource DifficultyConverter}}" FontWeight="SemiBold"/>
                                    </TextBlock>
                                </StackPanel>
                                
                                <!-- 描述和操作按钮 -->
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" 
                                             Text="{Binding Description}" 
                                             Style="{StaticResource ExamInfoStyle}"
                                             TextWrapping="Wrap"
                                             VerticalAlignment="Center"/>
                                    
                                    <Button Grid.Column="1" 
                                            Command="{Binding DataContext.StartExamCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Content="开始考试"
                                            Style="{StaticResource StartExamButtonStyle}"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- 加载指示器 -->
        <Grid Grid.Row="2" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#80FFFFFF">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,10"/>
                <TextBlock Text="正在加载考试列表..." FontSize="14" Foreground="#7F8C8D"/>
            </StackPanel>
        </Grid>
        
        <!-- 空状态 -->
        <Grid Grid.Row="2" 
              Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="📝" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="暂无可参加的考试" FontSize="16" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                <TextBlock Text="请联系老师发布考试" FontSize="14" Foreground="#BDC3C7" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>