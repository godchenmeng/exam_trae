<Window x:Class="ExamSystem.WPF.Views.UserEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑用户" Height="360" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Text="编辑用户信息" FontSize="16" FontWeight="Bold"/>

    <Grid Grid.Row="1" Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="用户名" VerticalAlignment="Center"/>
      <TextBox x:Name="UsernameTextBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="2" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="真实姓名" VerticalAlignment="Center"/>
      <TextBox x:Name="RealNameTextBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="3" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="邮箱" VerticalAlignment="Center"/>
      <TextBox x:Name="EmailTextBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="4" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="电话" VerticalAlignment="Center"/>
      <TextBox x:Name="PhoneTextBox" Grid.Column="1"/>
    </Grid>

    <Grid Grid.Row="5" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="角色" VerticalAlignment="Center"/>
      <ComboBox x:Name="RoleComboBox" Grid.Column="1" MinWidth="140">
        <ComboBoxItem Content="管理员" Tag="Admin"/>
        <ComboBoxItem Content="教师" Tag="Teacher"/>
        <ComboBoxItem Content="学生" Tag="Student"/>
      </ComboBox>
    </Grid>

    <CheckBox x:Name="IsActiveCheckBox" Grid.Row="6" Content="启用" Margin="0,8,0,0"/>

    <TextBlock x:Name="ErrorText" Grid.Row="6" Margin="120,8,0,0" Foreground="Red" TextWrapping="Wrap"/>

    <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="保存" Width="80" Margin="0,0,8,0" Click="OnSaveClick"/>
      <Button Content="取消" Width="80" Click="OnCancelClick"/>
    </StackPanel>
  </Grid>
</Window>