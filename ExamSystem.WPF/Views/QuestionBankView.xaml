<UserControl x:Class="ExamSystem.WPF.Views.QuestionBankView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <UserControl.Resources>
        <!-- 样式定义 -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#21618C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDC3C7"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#E74C3C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C0392B"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A93226"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#27AE60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#229954"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1E8449"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题和工具栏 -->
        <Border Grid.Row="0" Background="#F8F9FA" Padding="20,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 标题 -->
                <TextBlock Grid.Row="0" Text="题库管理" Style="{StaticResource HeaderTextStyle}" FontSize="20"/>

                <!-- 工具栏 -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 搜索框 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBox x:Name="SearchTextBox" 
                                Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                                Width="300" Height="35" 
                                Padding="10,8"
                                FontSize="14"
                                VerticalContentAlignment="Center"
                                BorderBrush="#BDC3C7"
                                BorderThickness="1">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                        <Button Content="搜索" 
                                Command="{Binding SearchCommand}"
                                Style="{StaticResource ButtonStyle}"
                                Margin="10,0,0,0"/>
                        <Button Content="刷新" 
                                Command="{Binding RefreshCommand}"
                                Style="{StaticResource ButtonStyle}"/>
                    </StackPanel>

                    <!-- 操作按钮 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="新建题库" 
                                Command="{Binding CreateQuestionBankCommand}"
                                Style="{StaticResource SuccessButtonStyle}"
                                IsEnabled="{Binding CanCreateQuestionBank}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 题库列表 -->
            <Border Grid.Column="0" BorderBrush="#E5E5E5" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 题库列表标题 -->
                    <Border Grid.Row="0" Background="#F8F9FA" Padding="15,10" BorderBrush="#E5E5E5" BorderThickness="0,0,0,1">
                        <TextBlock Text="题库列表" Style="{StaticResource HeaderTextStyle}" Margin="0"/>
                    </Border>

                    <!-- 题库列表内容 -->
                    <ListBox Grid.Row="1" 
                            ItemsSource="{Binding QuestionBanks}"
                            SelectedItem="{Binding SelectedQuestionBank}"
                            BorderThickness="0"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="15,10" BorderBrush="#E5E5E5" BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" 
                                                  Text="{Binding Name}" 
                                                  FontWeight="Bold" 
                                                  FontSize="14"
                                                  TextWrapping="Wrap"/>
                                        
                                        <TextBlock Grid.Row="1" 
                                                  Text="{Binding Description}" 
                                                  FontSize="12" 
                                                  Foreground="#7F8C8D"
                                                  TextWrapping="Wrap"
                                                  Margin="0,5,0,0"
                                                  Visibility="{Binding Description, Converter={x:Static local:Converters.StringToVisibilityConverter}}"/>
                                        
                                        <TextBlock Grid.Row="2" 
                                                  FontSize="11" 
                                                  Foreground="#95A5A6"
                                                  Margin="0,5,0,0">
                                            <Run Text="创建时间: "/>
                                            <Run Text="{Binding CreatedAt, StringFormat=yyyy-MM-dd}"/>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- 题库操作按钮 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
                        <Button Content="编辑" 
                                Command="{Binding EditQuestionBankCommand}"
                                Style="{StaticResource ButtonStyle}"
                                IsEnabled="{Binding CanEditQuestionBank}"/>
                        <Button Content="删除" 
                                Command="{Binding DeleteQuestionBankCommand}"
                                Style="{StaticResource DangerButtonStyle}"
                                IsEnabled="{Binding CanDeleteQuestionBank}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 分隔线 -->
            <GridSplitter Grid.Column="1" 
                         Width="5" 
                         HorizontalAlignment="Stretch" 
                         Background="#E5E5E5"/>

            <!-- 题目列表 -->
            <Border Grid.Column="2" BorderBrush="#E5E5E5" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 题目列表标题 -->
                    <Border Grid.Row="0" Background="#F8F9FA" Padding="15,10" BorderBrush="#E5E5E5" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" 
                                      Text="{Binding SelectedQuestionBank.Name, StringFormat='题目列表 - {0}', FallbackValue='题目列表'}" 
                                      Style="{StaticResource HeaderTextStyle}" 
                                      Margin="0"/>
                            
                            <Button Grid.Column="1" 
                                   Content="新建题目" 
                                   Command="{Binding CreateQuestionCommand}"
                                   Style="{StaticResource SuccessButtonStyle}"
                                   IsEnabled="{Binding CanCreateQuestion}"
                                   Margin="0"/>
                        </Grid>
                    </Border>

                    <!-- 题目列表内容 -->
                    <DataGrid Grid.Row="1" 
                             ItemsSource="{Binding Questions}"
                             SelectedItem="{Binding SelectedQuestion}"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             GridLinesVisibility="Horizontal"
                             HeadersVisibility="Column"
                             BorderThickness="0"
                             RowHeaderWidth="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="题目标题" Binding="{Binding Title}" Width="*" MinWidth="200"/>
                            <DataGridTextColumn Header="题型" Binding="{Binding QuestionType}" Width="80"/>
                            <DataGridTextColumn Header="难度" Binding="{Binding Difficulty}" Width="60"/>
                            <DataGridTextColumn Header="分值" Binding="{Binding Points}" Width="60"/>
                            <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}" Width="100"/>
                        </DataGrid.Columns>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Height" Value="40"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F8F9FA"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>

                    <!-- 题目操作按钮 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="15,10" HorizontalAlignment="Right">
                        <Button Content="编辑题目" 
                                Command="{Binding EditQuestionCommand}"
                                Style="{StaticResource ButtonStyle}"
                                IsEnabled="{Binding CanEditQuestion}"/>
                        <Button Content="复制题目" 
                                Command="{Binding DuplicateQuestionCommand}"
                                Style="{StaticResource ButtonStyle}"
                                IsEnabled="{Binding IsQuestionSelected}"/>
                        <Button Content="删除题目" 
                                Command="{Binding DeleteQuestionCommand}"
                                Style="{StaticResource DangerButtonStyle}"
                                IsEnabled="{Binding CanDeleteQuestion}"/>
                    </StackPanel>

                    <!-- 空状态提示 -->
                    <Border Grid.Row="1" 
                           Visibility="{Binding IsQuestionBankSelected, Converter={x:Static local:Converters.BooleanToVisibilityConverter}, ConverterParameter=Invert}">
                        <TextBlock Text="请选择一个题库查看题目" 
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"
                                  FontSize="16" 
                                  Foreground="#7F8C8D"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#F8F9FA" Padding="20,10" BorderBrush="#E5E5E5" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                          Text="{Binding StatusMessage}" 
                          VerticalAlignment="Center"
                          Foreground="#7F8C8D"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ProgressBar Width="20" Height="20" 
                                IsIndeterminate="True"
                                Visibility="{Binding IsLoading, Converter={x:Static local:Converters.BooleanToVisibilityConverter}}"
                                Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding QuestionBanks.Count, StringFormat='题库数量: {0}'}" 
                              Margin="0,0,20,0"
                              Foreground="#7F8C8D"/>
                    <TextBlock Text="{Binding Questions.Count, StringFormat='题目数量: {0}'}" 
                              Foreground="#7F8C8D"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>